# @cue/mobile

**iOS + Android** app via **Capacitor**, which wraps the web UI in a native shell and provides native APIs (notifications, camera for QR, secure storage).

Imports `@cue/engine` + `@cue/ui` — same code as the web app, native container.

Status: not started (post Phase 0 web).

{
  "name": "@cue/mobile",
  "version": "0.0.0",
  "private": true,
  "description": "Cue mobile shell — the same local-first app wrapped by Capacitor for iOS and Android.",
  "scripts": {
    "build:web": "pnpm --filter @cue/web build",
    "sync": "pnpm run build:web && cap sync",
    "android": "cap open android",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "typescript": "^5.4.0"
  }
}

# @cue/web

The **browser app** — Vite + React. The **first surface** built (Phase 0).

Imports `@cue/engine` + `@cue/ui`. Persists to IndexedDB. Runs fully local-first, no server.

Status: Phase 0 — capture + queue/process loop.

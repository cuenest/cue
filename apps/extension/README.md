# @cue/extension

**Browser extension** (Chrome / Firefox / Safari) via **WXT**. Quick-capture from anywhere on the web — right-click or popup — straight into your Inbox.

Imports `@cue/engine` + `@cue/ui`, reusing the same React components.

Status: not started (post Phase 0 web).

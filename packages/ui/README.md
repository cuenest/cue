# @cue/ui

Shared **React components** used across all surfaces (web, mobile, desktop, extension): Capture input, Focus view, Inbox list, etc.

Depends on `@cue/engine` for state and logic; contains presentation only. Extracted/grown as the web app stabilizes so other surfaces can reuse it.

Status: not implemented.
